{% extends 'base.html.twig' %}


{% block body %}

    <h1> Connexion </h1>
    <div class="container">
        <div class="col-4 mx-auto">
            
            <form action="{{ path('security_login')}}" method="post" class="text-center border border-light p-5">

                <p class="h4 mb-4">Connectez vous</p>

                <!-- Email -->
                <input type="email" class="form-control mb-4" placeholder="Entrez votre email" name="_username" required>

                <!-- Password -->
                <input type="password" class="form-control mb-4" placeholder="Entrez votre mot de passe " name="_password" required>

                <!-- Sign in -->
                <button class="btn btn-info btn-block my-4" type="submit">Se connecter</button>

                <!-- Register -->
                <p>Vous n'êtes pas membre ? 
                    <a href="{{ path('security_registration') }}">S'inscrire</a>
                </p>
            </form>
        </div>
    </div>


   
{% endblock %}
